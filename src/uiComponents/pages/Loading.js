import React from 'react';
import { LoadingPageContainer } from "../elements";
import LoadingContent from '../containers/Loading';

export default props => (
    <LoadingPageContainer>
        <LoadingContent />
    </LoadingPageContainer>
);